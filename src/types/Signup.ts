export type IsSignupDataCorrect = {
    firstName: boolean,
    lastName: boolean,
    email: boolean,
    password: boolean
}

export type DataToSignup = {
    firstName: string,
    lastName: string,
    email: string,
    password: string,
    id: string
}